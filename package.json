{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "node ./scripts/start.js",
    "start:all": "lerna run start --parallel --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
