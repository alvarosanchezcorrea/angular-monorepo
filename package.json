{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "node ./scripts/start.js",
    "start:all": "lerna run start --parallel --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.0.2"
  },
  "dependencies": {
    "pidtree": "^0.6.0"
  }
}
